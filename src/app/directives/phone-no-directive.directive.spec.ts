import { PhoneNoDirectiveDirective } from './phone-no-directive.directive';

describe('PhoneNoDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new PhoneNoDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
